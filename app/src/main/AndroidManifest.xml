<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_tut_wuri"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_tut_wuri"
        android:supportsRtl="true"
        android:theme="@style/Theme.Elearning_SD_Negeri_Sukorame_1"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.login.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Elearning.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.home.HomeActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        <activity
            android:name=".ui.cbt.CbtActivity"
            android:exported="false" />
        <activity
            android:name=".ui.list.ListActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/Theme.Elearning.Login"
            android:exported="false" />

    </application>

</manifest>
